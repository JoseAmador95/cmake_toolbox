@PACKAGE_INIT@

set_and_check(cmake_toolbox_MODULE_DIR "${CMAKE_CURRENT_LIST_DIR}")

if(NOT cmake_toolbox_MODULE_DIR IN_LIST CMAKE_MODULE_PATH)
    list(PREPEND CMAKE_MODULE_PATH "${cmake_toolbox_MODULE_DIR}")
endif()
