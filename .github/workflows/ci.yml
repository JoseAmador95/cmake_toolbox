name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test-linux:
    name: test-linux
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Validate checkout and submodules
        run: |
          git rev-parse --is-inside-work-tree
          git submodule status --recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq clang-format gcovr

      - name: Verify required tools
        run: |
          for tool in cmake ctest jq clang-format gcovr; do
            if ! command -v "$tool" >/dev/null 2>&1; then
              echo "Missing required tool: $tool" >&2
              exit 1
            fi
          done

      - name: Print tool versions
        run: |
          cmake --version
          ctest --version
          jq --version
          clang-format --version
          gcovr --version

      - name: Configure
        run: cmake -S . -B build -DCMAKE_TOOLBOX_BUILD_EXAMPLES=ON

      - name: Build
        run: cmake --build build

      - name: Run tests
        run: ctest --test-dir build --output-on-failure

      - name: Upload CTest log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ctest-linux-log
          path: build/Testing/Temporary/LastTest.log
          if-no-files-found: ignore

  test-macos:
    name: test-macos
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Validate checkout and submodules
        run: |
          git rev-parse --is-inside-work-tree
          git submodule status --recursive

      - name: Install dependencies
        run: |
          brew install jq clang-format
          if ! brew list gcovr >/dev/null 2>&1; then
            brew install gcovr
          fi
          if ! command -v gcovr >/dev/null 2>&1; then
            brew install pipx
            pipx ensurepath
            pipx install gcovr
            echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          fi
          if ! command -v gcovr >/dev/null 2>&1; then
            echo "Failed to install required tool: gcovr" >&2
            exit 1
          fi

      - name: Verify required tools
        run: |
          for tool in cmake ctest jq clang-format gcovr; do
            if ! command -v "$tool" >/dev/null 2>&1; then
              echo "Missing required tool: $tool" >&2
              exit 1
            fi
          done

      - name: Print tool versions
        run: |
          cmake --version
          ctest --version
          jq --version
          clang-format --version
          gcovr --version

      - name: Configure
        run: cmake -S . -B build -DCMAKE_TOOLBOX_BUILD_EXAMPLES=ON

      - name: Build
        run: cmake --build build

      - name: Run core tests
        run: |
          ctest --test-dir build --output-on-failure \
            -R "policy_|clangformat_|compilecommands_|discover_tests_|gcov_|integration_"

      - name: Upload CTest log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ctest-macos-log
          path: build/Testing/Temporary/LastTest.log
          if-no-files-found: ignore

  lint-basic:
    name: lint-basic
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Configure install-only project
        run: cmake -S . -B build-lint -DCMAKE_TOOLBOX_BUILD_EXAMPLES=OFF

      - name: Validate install rules
        run: cmake --install build-lint --prefix "$RUNNER_TEMP/install"
